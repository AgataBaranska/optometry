import { AppointmentReasonsResolver } from './appointment-reasons-resolver';

describe('AppointmentReasonsResolver', () => {
  it('should create an instance', () => {
    expect(new AppointmentReasonsResolver()).toBeTruthy();
  });
});
