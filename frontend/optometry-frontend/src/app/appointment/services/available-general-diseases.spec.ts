import { AvailableGeneralDiseases } from './available-general-diseases';

describe('AvailableGeneralDiseases', () => {
  it('should create an instance', () => {
    expect(new AvailableGeneralDiseases()).toBeTruthy();
  });
});
