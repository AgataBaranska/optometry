import { NewAppointmentDTO } from './new-appointment-dto';

describe('NewAppointmentDTO', () => {
  it('should create an instance', () => {
    expect(new NewAppointmentDTO()).toBeTruthy();
  });
});
